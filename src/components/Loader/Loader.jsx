import LinearProgress from "@mui/material/LinearProgress";

const Loader = () => {
  return <LinearProgress color="primary" fourColor variant="indeterminate" sx={{mt:2}} />;
};

export default Loader;
